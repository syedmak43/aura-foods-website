version: "3.8"

services:
  aura-foods:
    build: .
    container_name: aura-foods-website
    ports:
      - "3000:8000"
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
